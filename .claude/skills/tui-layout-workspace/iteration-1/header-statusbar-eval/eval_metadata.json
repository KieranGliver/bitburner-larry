{
  "eval_id": 1,
  "eval_name": "header-statusbar-eval",
  "prompt": "I've got a notes TUI app in Go using bubbletea v2 (charm.land imports). Right now the list, title input, and body textarea are all full-screen and I swap between them with a state uint. I want to add a persistent header at the top that shows 'NOTES' in a purple background, and a status bar at the bottom that shows the current mode (LIST / TITLE / BODY) and the relevant key hints for that mode. The header and status bar should stay visible regardless of which view is active, and everything should adapt if the terminal is resized.",
  "assertions": [
    "Uses tea.WindowSizeMsg to store width and height on the model",
    "Defines a header style with a background color using lipgloss.NewStyle()",
    "Defines a status bar style using lipgloss.NewStyle()",
    "Uses lipgloss.JoinVertical to compose header + body + status bar",
    "Status bar content varies by current state (shows different text for listView vs titleView vs bodyView)",
    "Sets Width(m.width) on header and/or status bar so they fill the terminal width",
    "Uses correct v2 import paths (charm.land/bubbletea/v2 and charm.land/lipgloss/v2)",
    "Body height accounts for header and status bar heights"
  ]
}
